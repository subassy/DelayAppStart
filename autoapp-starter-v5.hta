<html> 

<head>

<title>auto-app-starter v5</title>

<HTA:APPLICATION
  APPLICATIONNAME="Auto-appstarter-v4"
  VERSION="4"
  CONTEXTMENU="yes"/>    

 

<style>
ul {
	background-color: #584f4d;
	border:none;
	padding: 0px;
	margin: 0px;
}

li {
	background-color: #0000ff;
	color: #fff;
	border-top: 1px solid #fe9772;
	border-bottom: 1px solid #9f593f;
	font-size: 24px;
	letter-spacing: .05em;
	list-style-type: none;
	text-shadow: 2px 2px 1px #9f593f;
	height: 50px;
	padding-left: 1em;
	padding-top: 10px;
}
</style>


<script language=JavaScript>
    window.onload = function () {
        var height = screen.availHeight - 500;
        self.resizeTo(640, height);
        self.moveTo(100, 50);

        fnCreateSecondsDropDown();
    } 
 


    function fnCreateSecondsDropDown() {

        // this stuff seems to only run via an event, 
        // not just by calling the function
        // and the window.onload actually works as that event
/****************************** sample of working DOM adding ***********


        // grab the form by id
        var elGetFormID = document.getElementById('mainForm');
        // do some element creating
        var elSelectBox = document.createElement('select');
        var elOptBox = document.createElement('option');
        var newOptText = document.createTextNode('1');
        // set some attributes...
        elSelectBox.setAttribute('id', 'iSecBox');
        elOptBox.setAttribute('id', '1');
        elOptBox.setAttribute('value', '1');
        // wire everything up to the DOM so you can see it...
        elOptBox.appendChild(newOptText);
        elSelectBox.appendChild(elOptBox);
        elGetFormID.appendChild(elSelectBox);
*************************************************************/

       // var elGetFormID = document.getElementById('mainForm');
        //var elSelectBox = document.createElement('select');
        //elSelectBox.setAttribute('id', 'iSecBox');

        //var elOptBox = document.createElement('option');

        /*************************************************************/

        var GetSelBoxID = document.getElementById('iSecBox');
        /* 
        // possible alternative method of doing same 
        var TempNewOpt = document.createElement("option");
        TempNewOpt.text = "1";
        GetSelBoxID.add(Option, GetSelBoxID[0]);
        GetSelBoxID.options
        // possible alternative method of doing same
        // from http://www.w3schools.com/jsref/met_select_add.asp
        */

        for (var i = 0; i < 60; i++) {
            elAddOpt = new Option(i, i);
            GetSelBoxID.options[GetSelBoxID.options.length] = elAddOpt;
            elAddOpt.setAttribute('id', i);
            elAddOpt.setAttribute('value', 'i');
            
        /*************************************************************/
            //GetSelBoxID.options[]

            //elOptBox.setAttribute('id', i);
            //elOptBox.setAttribute('value', i);
          //  var newOptText = document.createTextNode(i);
            //elOptBox.appendChild(newOptText);
        }


        
   //     elAddOpt.appendChild(newOptText);





        // the end/outside the for-loop
        // wire everything up to the DOM so you can see it...

        //elSelectBox.appendChild(elOptBox);
        //elGetFormID.appendChild(elSelectBox);

    }

    function fnAddToPathList() {

        var sAppList = document.getElementById('PathList'); // point at the UL element via id
        
        var elem = document.getElementById("txtPathInputBox"); // Get text field contents
        
        var sPathInputContents = vbsTrimString(elem.value);

       // var sNewlyTrimmed = vbsTrimString(sPathInputContents);

        var newEl = document.createElement('li'); // create a bullet point for the UL above
        var newText = document.createTextNode(sPathInputContents); // contents for the the new li
        // note: no IE 8 js trim() function for strings :-(

        newEl.appendChild(newText); // put the contents in the bullet point...

        sAppList.appendChild(newEl);
        elem.value = "Enter a path";

    }

    //fnCreateSecondsDropDown();

</script>

<script language="vbscript">
    Function vbsTrimString(sPassString)
        vbsTrimString = Trim(sPassString)
    End Function
</script>
</head>
    <body>
    <div class="body">

        <p>
            <form id="mainForm">
        <input type="text" id="txtPathInputBox" value="Enter a path" size="50"  />

        <select id="iSecBox"></select>

                <!--
            <input type="button" value="Add" onclick="fnAddToPathList()">
                    -->
<span id=spHeader2 style="border: 2px solid green;   text-align:center align:center">
Add
</span>
            </form>


	</p>
        <p>
            <ul id="PathList">
                
            </ul>
        </p>
    </div>



<span id=spHeader style="border: 2px solid green; margin-right:10px; Width:250;  text-align:center align:center">
Select shortcuts to push
</span>

<span id=spHeader2 style="border: 2px solid green; Width:100;  text-align:center align:center">
Select to push
</span>

    </body>
</html>

<!--
<script>

/*
    var WshShell = new ActiveXObject("WScript.Shell");
    var sIEPath = "\"C:\\Program Files (x86)\\Internet Explorer\\iexplore.exe\"";
    var sSteamPath = "\"P:\\Program Files (x86)\\Steam\\Steam.exe\"";
    var sExplorerPath = "\"c:\\Windows\\explorer.exe\""
    var sPutyPortable = "\"S:\\My Dropbox\\PuTTYPortable\\PuTTYPortable.exe\""
*/


    /* *************************************************************** */
    // (don't delete this)
    // finally eliminated that extra 0 that started out the two arrays 
    //and it's working
    // to change it back: and a "0," to both of the below arrays
    // as the first element
    // then in the below two for-loops with i and j make the initial value of both
    // be set to 1 instead of 0
    // those two chagnes should make it work as before

    //only disadvantage to how it is now is the element counter starts at 0 instead of 1
    //outside that everything is the same

    /* *************************************************************** */





//    var arAppPaths = [sExplorerPath, sIEPath, sSteamPath, sPutyPortable];
//    var arAppTimes = [      3,           4,         5,             6    ];




    /*
    function fnRunExe(arAppPaths, iNumSec, iCounter) {
        var fnRunIt = function () {
            //if    (iTime < arAppPaths.length) {
            oTimer.innerHTML += "</br>Just ran: </br>" + arAppPaths[iCounter] + " with number of seconds " + iNumSec 
			+ "</br> also, counter is " + iCounter;
                    WshShell.run(arAppPaths[iCounter]);
            //} 
        }

        //var iTime =
            setTimeout(fnRunIt, iNumSec);
    }

    */
/* *************************************************************** */
    /*
    
    function iConvertSeconds(i) {
		if (i < 60) {
			return i * 1000;
		}
	}


    */
/* *************************************************************** */


    /*
	function fnForTheCalling() {
	    for (var i = 0; i < arAppTimes.length; i++) {
	        var iKeepTrack = 0;
	        for (var j = 0, iSumTotal = 0; j <= i; j++) {
	            iSumTotal += arAppTimes[j];
	        }
	        iKeepTrack = iConvertSeconds(iSumTotal);
	        fnRunExe(arAppPaths, iKeepTrack, i);
	    }

	}
    */
/* *************************************************************** */
//		found below on http://stackoverflow.com/questions/1230233/how-to-go-through-an-array-and-add-their-values
//		for (var k = 0, iSumTotal = 0; k < arAppPaths.length; iSumTotal += arAppTimes[k++] );


</script>

-->

